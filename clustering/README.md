Folder Navigation

1. cluster_models: topic modeling cluster summaries using abstracts. cluster hits were pulled via bridge-rna's cohort selection.
2. cluster_models_chars: topic modeling cluster summaries using characteristics. cluster hits were pulled via bridge-rna's cohort selection.
3. cluster_models_alt: topic modeling cluster summaries using alternate pre-processesing method. Uses the pre-preprocess method where each sample in a cohort retrieves 10 hits. (no pre-pooling)
4. deseq_summaries: deseq results dataframe using  bridge-rna's cohort selection's hits as input.
5. deseq_summaries_alt: deseq results dataframe using the alternate pre-processed hits. Hits use the pre-preprocess method where each sample in a cohort retrieves 10 hits. (no pre-pooling)
6. cluster_metadata.ipynb: Topic modeling
7. cluster_test: topic modeling test
8. deseq_hits: deseq pipeline
9. deseq_test: deseq test

