# Secrets and local configuration
.env
.env.*
!.env.example

# Python environments and generated files
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local editor/assistant state
.claude/
.serena/
.vscode/
.idea/

# Operating-system metadata
.DS_Store
Thumbs.db
Desktop.ini
*Zone.Identifier*

# Generated local output
reports/
*.log

# Manifold precompute artifacts. Everything under cache/ is reproducible from
# the precompute scripts and is too large to track: the OSDR expression matrix
# is ~130 MB, the ARCHS4 GEO metadata join ~33 MB, and the four coordinate
# parquets ~9-13 MB each.
cache/

# Candidate coordinate builds held against the shipped cache by
# `validate_artifacts.py --compare DIR`, such as the n_neighbors=15 UMAP that
# `--compare` scored and lost. Same reasoning as cache/: reproducible from the
# precompute scripts, and tens of MB per candidate.
cache_compare_*/
